#include <stdio.h>

/**
 * main - print base16
 *
 * Return: 0
 */
int main(void)
{
	char base16;

	for (base16 = '0'; base16 <= '9'; bse16++)
	{
		putchar(base16);
	}
	for (base16 = 'a'; base16 <= 'f'; base16++)
	{
		putchar(base16);
	}
	putchar(10);
	return (0);
}
